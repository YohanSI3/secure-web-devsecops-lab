# Rate limiting strict pour /api/auth/login (zone "login", voir
# conf.d/rate-limiting.conf). burst=3 : tolère un petit rattrapage (deux
# ou trois essais rapprochés, faute de frappe sur le mot de passe) sans
# laisser la voie libre à un brute-force.
limit_req zone=login burst=3 nodelay;
