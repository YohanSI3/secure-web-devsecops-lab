PORT=3000
DATABASE_URL=postgresql://secure_web_lab_app:changeme@127.0.0.1:5432/secure_web_lab
# Signe l'identifiant de session (pas les données de session elles-mêmes,
# stockées côté serveur dans PostgreSQL). Générer avec :
#   openssl rand -hex 32
SESSION_SECRET=changeme
